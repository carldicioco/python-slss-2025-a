def average(num: float, num_two: float, num_three: float):
    """Return the average of three given numbers"""
    return (num + num_two + num_three) / 3


results = average(104, 121, 112)
print(results)
